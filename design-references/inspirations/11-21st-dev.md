# Inspiration: 21st.dev

**Type:** LIVE BROWSE SOURCE (component gallery, React/Tailwind)
**Status:** USE — for patterns, via web browsing (no MCP connector needed).

**What it is:** A gallery of 400+ dashboard/UI components. Real examples of
sidebars (incl. nested/collapsible), KPI cards, stats-with-links, bento
dashboards, area/progress cards, empty states, tables, kanban, charts.

**Borrow:** layout, hierarchy, spacing, composition, interaction patterns.
Specific pulls already used: nested sidebar, KPI+links, card-expand, empty states.

**Avoid:** pasting its React/Tailwind CODE. Extract the pattern and REBUILD it in
JobSpike's vanilla CSS `--js-*` tokens. It's a reference, not a code source.
